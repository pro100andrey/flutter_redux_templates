<!-- generated by `frx flow --md` — do not edit, re-run it -->

# ForgotPasswordPage

`app/lib/connectors/forgot_password_page_connector.dart`

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant UI as ForgotPasswordPage
    participant VM as ForgotPasswordPageConnector
    participant A1 as SetEmailAction
    participant A2 as ForgotPasswordAction
    participant ST as AppState
    participant NAV as Router

    User->>UI: email.onChanged
    UI->>VM: email()
    VM->>A1: dispatchSync
    A1->>ST: copyWith(forgotPassword.email)

    User->>UI: onPressedResetPassword
    UI->>VM: onPressedResetPassword()
    VM->>A2: dispatch
    Note over A2: WaitingAction · async
    A2->>ST: copyWith(forgotPassword)
    A2--xVM: UserException

    User->>UI: onPressedBackToLogin
    UI->>VM: onPressedBackToLogin()
    VM->>NAV: GoAction.pop
```

| Interaction | Dispatches | Writes |
| --- | --- | --- |
| `email.onChanged` | `SetEmailAction` | `forgotPassword.email` |
| `onPressedResetPassword` | `ForgotPasswordAction` | `forgotPassword` |
| `onPressedBackToLogin` | `GoAction.pop` | — |

`?` marks a dispatch that only runs under a condition.

[← all flows](README.md)
